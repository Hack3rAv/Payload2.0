{
  "name": "payload-workers-backend",
  "version": "1.0.0",
  "description": "Cloudflare Workers backend for payload management system",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "db:generate": "wrangler d1 execute payload-db --file=./schema.sql",
    "db:local": "wrangler d1 execute payload-db --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "wrangler": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
